# 语音克隆模型核心依赖
# 这些是大型依赖，建议使用代理下载
# pip install -r requirements_model.txt --proxy socks5://192.168.0.221:8800

# PyTorch (约 2GB)
torch==2.6.0
torchaudio==2.6.0

# Transformers (约 500MB)
transformers==4.46.3
huggingface-hub==0.36.0
safetensors==0.5.3

# Chatterbox TTS 语音克隆模型
chatterbox-tts==0.1.6
resemble-perth==1.0.1

# 音频处理
librosa==0.11.0
soundfile==0.13.1
scipy==1.16.3
numpy==1.25.2
